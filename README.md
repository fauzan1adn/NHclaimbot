How to Run the Script:

- Make sure Python and Google Chrome are installed on your system.
- Open Terminal or Git Bash in the project folder and install the required packages:

Bash
pip install selenium requests

- Ensure the data.py file is located in the same directory as the main script.
- Run the script using: python main.py
- Enjoy your script
